/**
 * Clase que define el componente extension de zero
 * Autor: Jeremy Rodriguez Solorzano
 */

#include <iostream>
#include <string.h>

using namespace std;

class ExtZero {

    public:

        /**
         * Extender numero de 8b a 64b anyadiendo ceros a la derecha del bit menos significativo
         */
        string extZ(string n);
};